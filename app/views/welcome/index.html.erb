<div class="row welcome-index text-center">
 
  <div class="col-xs-12 col-md-8 col-md-offset-2">

    <div class="col-xs-12 jumbotron text-center">
      <h1>Not Wikipedia</h1>
      <h2>Like Wikipedia, Only Not.</h2>
      <h3>Create and Share <a href="https://guides.github.com/features/mastering-markdown/" target="_blank">Markdown</a> Wikis</h3>
    </div>

    <% if current_user && current_user.upgraded_account? %>
      <div class="col-xs-12">
        <h4> Thanks for hanging around. <br> Why don't you create a few wikis?</h4>
        <%= button_to "Create Wiki", 
                      new_wiki_path, 
                      class: "btn btn-primary btn-lg", 
                      method: :get %>
      </div>

    <% elsif current_user %>
        <div class="col-sm-6">
          <h4>Create as many public <br> wikis as you want!</h4>
          <%= button_to "Create Wiki", 
                        new_wiki_path, 
                        class: "btn btn-primary btn-lg vertical-bottom", 
                        method: :get %>
        </div>


        <div class="col-sm-6">
          <h4>Collaborate with friends on private wikis.</h4>
          <%= button_to "Premium Plan", 
                        new_subscription_path, 
                        class: "btn btn-primary btn-lg", 
                          method: :get %>
        </div>
      
    <% else %>
      <div class="col-sm-6">
        <h4>Create as many public <br> wikis as you want!</h4>
        <%= button_to "Free Plan", 
                      new_user_registration_path, 
                      class: "btn btn-primary btn-lg", 
                      method: :get %>
      </div>

      <div class="col-sm-6">
        <h4>Collaborate with friends <br> on private wikis!</h4>
        <%= button_to "Premium Plan", 
                      new_subscription_path, 
                      class: "btn btn-primary btn-lg", 
                        method: :get %>
      </div>
    <% end %>          
  </div>

</div>